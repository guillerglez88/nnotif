version: '3.4'

services:
  postgres:
    image: "postgres"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "nnotif"
    volumes:
      - "dbdata:/var/lib/postgresql/data:rw"
    ports:
      - "5432:5432"
volumes:
  dbdata: {}